from django.apps import AppConfig


# Configuração para o aplicativo 'authentication'.
class AuthenticationConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "authentication"
